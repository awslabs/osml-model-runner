---
# Build
#

stages:
  - build

build: 
  extends: .kaniko
  stage: build
  needs: []
  variables:
    # User defined
    TARGET: "runner"
    DOCKERFILE: "docker/Dockerfile"
    DESTINATION: ${MICROSERVICE_IMAGE}
